{
    "name": "quiz-app",
    "version": "1.0.0",
    "scripts": {
        "preinstall": "npm install --prefix quiz-server && npm install --prefix quiz-client && npm install -g angular-cli",
        "postinstall": "cd quiz-client &&  ng build && mv dist/* ."
    }
}