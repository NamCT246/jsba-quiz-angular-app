{
    "name": "quiz-app",
    "version": "1.0.0",
    "scripts": {
        "preinstall": "npm install --prefix quiz-server && npm install --prefix quiz-client",
        "postinstall": "cd quiz-client &&  ng build --aot -prod"
    }
}